# Job Hunter — Status

_Last updated: 2026-07-20 by Claude (weekly status review)._
_Seeded from git history + prior transcripts._

## Now (live on Railway, branch main)
- **Google Sign-In (OAuth 2.0)** — "Continue with Google" on `/login` + `/register` (server-rendered, so web **and** PWA share it). Full-page redirect flow (`/auth/google/start` → `/auth/google/callback`) with `state` + PKCE; verifies email is Google-verified, links to an existing account by email or creates a new one. Coexists with email/password (not removed). Needs Railway env `GOOGLE_CLIENT_ID` + `GOOGLE_CLIENT_SECRET`; redirect URI `https://web-production-192b7.up.railway.app/auth/google/callback`. New `users` columns `google_sub`/`auth_provider`/`avatar_url`.
- All LLM calls on Gemini (Anthropic fully removed) — apply form-filling, confirmation, CV, cover letters, search.
- **Multi-source `ingestion/` pipeline**: uniform `SourceAdapter` interface, free Big-Tech internal endpoints + paid aggregators (admin-only, gated), Pydantic normalization, multi-stage fuzzy dedup, fail-soft fallback to legacy dedup.
- **Relevance gate** (geography + whole-title match) runs on **every** inserted job, not just external sources — keeps Israel-scoped Product roles, drops off-title/abroad. `INGEST_RELEVANCE_GATE=0` / `BIGTECH_ENABLED=0` kill-switches.
- **Gemini scorer hardened**: `thinkingConfig:{thinkingBudget:0}` everywhere, batch 50→20, retain threshold 30%, and object-by-object `raw_decode` salvage that recovers valid jobs from truncated / missing-comma / trailing-junk responses (fixes "56 found, only 6 added").
- **Queue cleanup**: one-time guarded boot cleanup (per-flag-value token, no longer wipes fresh search results); SQLite `busy_timeout` 30s so reject/remove writes persist.
- **Apply engine DISABLED by default (2026-07-20)** — `APPLY_ENGINE_ENABLED` unset ⇒ auto-submit no-ops; re-enable with Railway env `APPLY_ENGINE_ENABLED=1`. Disabled pending 0-submit root-cause (see APPLY_ENGINE_INVESTIGATION.md). Apply mislabeling + apply-freeze fixed; approve no longer auto-fires the apply engine unless `auto_apply_enabled`.
- Manual-apply rows render **amber** ("Manual Apply Needed"); notification summary reads honestly + leads with results ("🎯 N new role(s) — Company1, Company2…").
- Analytics: approval = (approved+applied)/total suggested; **applied = applied/approved** (of jobs that cleared the gate, how many were applied to). [updated 2026-07-20]
- Sourcing prefers **direct-ATS URLs** over job-board aggregators. SPA queue actions: mark applied / remove / open, 6 reject reasons, apply-result detail + retry, score explainer, list sorting, view-CV PDF.
- **Apply engine hardened**: parked-link fix (no more false "Verified" on parked domains), timeout hardening, truthful submit-verification, auto-answers required questions, failure diagnostics + removal reasons, push self-heal.
- **Admin panel** (admin-only): mobile Settings → Admin modal + `/admin` panel — queue overview, maintenance (clear attempted/applied, rescore, dedup), users; new admin endpoints; queue-stats use `get_stats` so Passed/Total match the dashboard. SW cache bumped to v7.
- **Queue durability**: legacy modal adds broken-link / job-gone reasons; redeploys no longer resurface handled jobs; one-time prune of already-attempted jobs.
- HEAD `ee70390` (Google Sign-In). Full suite 116 passing.

## ⚠️ Folder hygiene (do this)
The OneDrive copy (`…\Eran's dev\Job Hunter`, branch `master`, commit `870c4eb`) has drifted. Salvage any unsynced edits, then archive it. See `migration-plans\PLAN_unify-job-hunter.md`.

## Next (confirm priority)
- **Run `GET /api/admin/apply-selftest` on prod** (logged in as admin) and paste the JSON back — it will pinpoint why the apply engine submits nothing (browser launch error vs missing GEMINI key vs missing CV vs empty queue). Do this BEFORE any more apply-engine code changes.
- **Remove the `QUEUE_CLEANUP` env var** on Railway — the boot cleanup is now self-guarding per-flag-value and no longer needs it. (Leaving it set is safe but pointless.)
- Verify direct-ATS sourcing + the new ingestion pipeline actually fill the Approved queue with auto-submittable jobs (open question; may be stale now that the relevance gate + Big-Tech sources landed — confirm with a live run).
- Optionally enable `APPLY_RESOLVE_CAREER_PAGE=1` and measure rescue rate on LinkedIn listings.
- Live-validate the Big-Tech endpoints that were failing: Google v3 (404) / Apple (301→pagenotfound) / Microsoft — Amazon works.

## Known sharp edges
Two-folder drift (being retired); sandbox can't push; Playwright browser binaries must be present in the Railway image for real submits.

## Changelog (newest first)
- 2026-07-20 (round 9) — **Resolver precision: title + location.** Live probe resolved Wiz but to the WRONG posting ("Product Manager" matched "Product **Marketing** Manager" @ 100, Remote-USA) — `token_set_ratio` scores a subset as perfect. Fixes: (a) `_title_ratio` now strips seniority/generic words then uses `token_SORT_ratio`, so "Senior/Group Product Manager"==「Product Manager」 but "Product Marketing Manager" (75) / "Product Designer" (71) are rejected; (b) `_loc_match` + location-aware ranking picks the posting in the target location when a board lists the same title in several places; (c) threaded the job's location through `submit_application`/`_submit_application_guarded`/both call sites, and added `&location=` to the resolver probe. +4 tests, suite **142→145**.
- 2026-07-20 (round 8) — **Resolver made Gemini-free (root cause: Gemini 429).** `gemini_raw` exposed the real failure: `HTTP 429 Too Many Requests` — the prod Gemini key is rate-limited/quota-capped, so the board-hint always failed. Added a curated, live-verified `_ATS_BOARD_MAP` (Wiz→wizinc, Gong→gongio, Payoneer→payoneer, Cato→catonetworks, Taboola, Similarweb, Fireblocks, AppsFlyer, Melio, Yotpo, Lemonade(Ashby), JFrog) checked FIRST — deterministic, zero Gemini. Name-slug guessing next, Gemini only as best-effort fallback (429-tolerant). +2 tests, suite **140→142**. ⚠️ Broader flag: the Gemini 429 likely also degrades job SCORING in search — consider a higher Gemini quota / paid tier or global 429 backoff.
- 2026-07-20 (round 7) — **Fix Gemini board-hint (was returning []).** Probe trace showed Railway reaches Greenhouse fine (stripe 520) and the key is present, but `gemini_slugs: []` — so `wizinc` was never tried. Root cause: `_gemini_board_candidates` required a strict JSON-array parse (`_claude_json`), which silently yields [] on any prose/format drift or a swallowed API error. Rewrote it to call `_claude` raw and extract slugs shape-tolerantly (quoted tokens → bare-word fallback, stop-word filtered), still verified against the live ATS API. Added `gemini_raw` to the resolver probe to expose the actual model output. +3 tests, suite **137→140**.
- 2026-07-20 (round 6) — **Enriched resolver probe (instrument the null).** Wiz/Gong still resolved null in prod despite confirmed-reachable Greenhouse boards (wizinc/gongio) from the dev sandbox — so the probe now returns the full trace: `name_slugs`, `gemini_slugs` (proves Gemini works in prod + what it returns), `greenhouse_reachable_stripe_jobs` (proves whether Railway's egress IP can reach the ATS APIs at all — cloud IPs are sometimes blocked where a dev box isn't), and per-slug `slug_posting_counts`. One probe now pinpoints: not-deployed vs Gemini-empty vs Railway-egress-blocked vs title-threshold. If egress is blocked, the fix is to route `_ats_get` through the existing ingestion ProxyManager.
- 2026-07-20 (round 5) — **ATS resolver: Gemini-suggested board slugs.** Live probing showed name-munging alone resolves too few companies (JFrog ✅ but Wiz/Gong/monday ✗) because boards live under non-obvious slugs (Wiz→`wizinc` 143 jobs, Gong→`gongio` 102 jobs). Added `_gemini_board_candidates()` — Gemini suggests likely board slugs, each VERIFIED by probing the live ATS API (wrong guess = no postings = harmless). `resolve_ats_application` now scans name-slugs first, then Gemini slugs (`APPLY_RESOLVE_LLM_SLUGS=1` default). Known gap: Comeet-UID companies (e.g. monday.com) still won't resolve (Comeet needs a token, not a slug). +3 tests, suite **134→137**.
- 2026-07-20 (round 4) — **Apply engine: deterministic ATS resolver (steps 2–4).** `queue_audit` proved the real cause: of 2 approved jobs, **0 auto-submittable** (1 job-board, 1 attempts-exhausted) — the engine had nothing to apply to. New `apply_engine.resolve_ats_application(company, title)` resolves company → known ATS board (Greenhouse/Lever/SmartRecruiters/Ashby/Comeet via their JSON APIs) → exact posting by fuzzy title match — NO Google scrape. Wired into `submit_application` ahead of the manual-bail (`APPLY_RESOLVE_ATS=1` default). Added read-only resolver probe to the self-test: `?company=<>&title=<>`. +6 tests, suite **129→134**. Next: sourcing fill so the approved queue actually contains resolvable direct-ATS jobs (SecretJobs + more boards), then a guarded live single-apply with the engine re-enabled.
- 2026-07-20 (round 3) — **Apply-engine self-test result + SecretJobs adapter.** SELF-TEST (prod): playwright_importable ✅, chromium launched+navigated ✅ (v131), GEMINI key ✅, volume ✅ (`/data/jobs.db` + CV present) — **infra fully healthy, disproves the no-volume / no-browser / no-key hypotheses.** So "applies nothing" is now a QUEUE-COMPOSITION question → extended `apply-selftest` with a read-only `queue_audit` (approved_total / auto_submittable / job_board_manual_by_design / dead_link / flagged_manual_required / attempts_exhausted). SECRETJOBS: new PAID admin-only authenticated adapter (`ingestion/adapters/secretjobs.py`) — no public feed, reads the account holder's own session via `SECRETJOBS_JOBS_URL` + `SECRETJOBS_AUTH_HEADER` + `SECRETJOBS_AUTH_VALUE` (Railway env; token never in code), shape-tolerant parser; registered PAID in the ingestion registry. +4 tests, suite **125→129**.
- 2026-07-20 (round 2) — **Bad-link fixes + apply-engine live self-test.** LINKS: new `apply_engine._looks_closed` detects 200-OK "closed/filled/expired" ATS pages (Greenhouse/Lever/Comeet/Ashby/Workday) — wired into `check_url_alive` + `_link_status`; the search re-validation pass now **auto-removes** definitively-dead review-queue rows (status new/approved → rejected; `LINK_AUTOREJECT_DEAD=0` to disable); blank-location postings are now DROPPED for concrete-location users (`INGEST_STRICT_LOCATION=1` default; `=0` restores permissive). APPLY: new admin `GET /api/admin/apply-selftest` — read-only prod probe (Playwright import, live Chromium launch + example.com nav, GEMINI key bool, DB/CV presence, kill-switch state); submits nothing. +8 tests, suite **117→125**. Dockerfile note: `playwright install chromium` runs WITHOUT `--with-deps` and `PLAYWRIGHT_AVAILABLE` only reflects the import, not a runtime-findable browser — prime suspect for "applies nothing regardless of code"; self-test will confirm.
- 2026-07-20 — **Applied% fix + apply-engine kill-switch + investigation plans.** (1) Applied rate now = **applied/approved** (was applied/total-suggested); card relabeled "N applied of N approved". `web/src/App.tsx`; rebuilt `web_bundle` (CSS 26KB), SW **v7→v8**. (2) Apply engine **disabled by default** via `APPLY_ENGINE_ENABLED` kill-switch at `apply_engine.submit_application` + both entry points (`run_job_apply`, `_trigger_apply_bg`) — set Railway env `APPLY_ENGINE_ENABLED=1` to re-enable (no redeploy). New test `test_apply_engine_disabled_by_default`; suite **116→117**. (3) Root-caused the expired/wrong-location-jobs issue (blank-location gate bypass + `check_url_alive` can't see 200-OK "position closed" pages + sticky `url_verified`) and re-confirmed the apply-0-submits causes; execution plans delivered.
- 2026-07-20 — Weekly status review: second quiet week — no commits since Google Sign-In (`ee70390`, 07-09); STATUS already in sync. Untracked `PAID_SOURCES_BENCHMARK.md` still in tree. Still-open ops unchanged: register the 2 Google OAuth env vars on Railway + remove the `QUEUE_CLEANUP` env var. (Collapsed the 07-06/07-12/07-13 review entries.)
- 2026-07-12 — Weekly status review: back-filled the missing 2026-07-07 batch into Now/changelog; synced HEAD `721b909`→`ee70390`, suite 102→116.
- 2026-07-07 — **Apply engine + admin panel + queue durability.** Apply: parked-link fix (stop false "Verified"), timeout hardening, truthful submit verify, auto-answer required questions, failure diagnostics, removal reasons, push self-heal. Admin-only panel (mobile Settings → Admin modal + `/admin`): queue overview, maintenance (clear attempted/applied, rescore, dedup), users, new admin endpoints; queue-stats via `get_stats` so Passed/Total match dashboard. Legacy modal: broken-link/job-gone reasons; redeploys stop resurfacing handled jobs; one-time prune of attempted jobs. SW cache v6→v7; SPA bundle rebuilt.
- 2026-07-09 — **Google Sign-In added (web + PWA).** New server-side OAuth 2.0 authorization-code flow (redirect-based, PWA-safe — no GIS JS button). `db.py`: added `users.google_sub` (partial-unique index), `auth_provider`, `avatar_url`. `auth.py`: `find_or_create_google_user()` — match by sub → link by verified email → create (random unusable password hash; admin email keeps admin role; creates profile row). `app.py`: `GOOGLE_CLIENT_ID/SECRET` config + `/auth/google/start` (state cookie + S256 PKCE) + `/auth/google/callback` (state check, token+userinfo exchange via urllib, session cookie, `/onboarding` for new users else `/dashboard`); "Continue with Google" button on login+register. `.env.example` documented. Tests: `tests/test_google_auth.py` (5). Full suite **116 passed**. **Deploy needs 2 Railway env vars** (`GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`) + the redirect URI registered in Google Cloud Console. No `web_bundle` rebuild (login is server-rendered).
- 2026-06-29 — **Fix persistent 'database is locked' (every action failing).** Root cause: a connection somewhere held the SQLite writer lock open (leaked open transaction). Structural fix: `db.get_db()` now uses **autocommit** (isolation_level=None) so every write commits + releases the lock immediately — a leaked/abandoned connection can never starve writers; explicit commits are no-ops; WAL readers unaffected. Also fixed 3 CV-optimizer endpoints that did `with get_db() as c` (commits but never CLOSES → connection leak) and made the one-time boot cleanup close its connection on error. Stress-tested: 700 concurrent writes (search burst + 8 users) → 0 lock errors, all rows persisted. Suite 102 passed. NOTE: restart the Railway service once to clear the currently-held lock from the old code.
- 2026-06-29 — **Scorer JSON salvage hardened.** Replaced 'slice-to-last-brace' with an object-by-object `json.JSONDecoder().raw_decode` loop that keeps every object that parses and stops at the first broken one — recovers valid jobs from truncation, missing commas between objects, and trailing prose junk. Suite 102 passed.
- 2026-06-28 — **Scorer recovery + relevance + notifications (consolidated).** (a) Gemini scoring was losing whole 50-job batches to token-limit truncation → `_parse_scored_response` salvages every complete object, batch 50→20, retain threshold 40→30% (root cause of "56 found, only 6 added"). (b) Relevance gate (geo + WHOLE-title fuzzy≥80) now runs on **every** inserted job, not just external sources — drops off-title roles (Sales Manager, Cloud Architect) that were leaking into notifications. (c) Search push notification leads with results: "🎯 N new role(s) — Company1, Company2…". Suite 102 passed.
- 2026-06-28 — **Big-Tech sources Israel-scoped + opt-in.** Geo gate is Israel-city-aware; work-mode words ('hybrid'/'remote') no longer count as a location; remote-anywhere passes only if the user lists it. Adapters query a real location (default Israel); `BIGTECH_ENABLED` gate + `INGEST_RELEVANCE_GATE=0` kill-switches. Known: Google v3 (404) / Apple (301) / Microsoft endpoints still need live validation; Amazon works.
- 2026-06-28 — **Queue cleanup (consolidated).** Auth-free one-time boot cleanup marks queued jobs 'applied (manual)' except an ordered-substring keep-list; runs once **per flag value** (`queue_cleanup_token`) so it never wipes fresh search results; cleanup runs LAST in startup and clears `apply_error`. SQLite `busy_timeout` 10s→30s so reject/remove writes persist (root cause of "removed jobs come back"). ⚠️ Now self-guarding — **remove the `QUEUE_CLEANUP` env var.**
- 2026-06-28 — **Analytics rates fixed.** Both now over total suggested: approval = (approved+applied)/total, applied = applied/total. Rebuilt SPA bundle (25KB Tailwind CSS).
- 2026-06-28 — **Apply engine decoupled from review.** Approve no longer fires the apply engine unless `auto_apply_enabled`. `thinkingConfig:{thinkingBudget:0}` applied to `apply_engine.py` Gemini helper so extract/form-fill/confirmation stop returning 'No JSON found'.
- 2026-06-28 — **Prod-log bugfix round.** Fixed `_LV_COMPANIES` UnboundLocalError (Lever discovery crashed every run); `thinkingBudget:0` on all 3 scoring calls; quieted benign httpx/access-log noise that Railway was tagging red.
- 2026-06-28 — **New `ingestion/` pipeline.** Modular `SourceAdapter` interface: free Big-Tech internal endpoints (all users) + paid aggregators (admin-only, gated by `users.role`). Pydantic `NormalizedJob`→`CanonicalJob` normalization; multi-stage fuzzy deduper (canonical-URL → fingerprint → rapidfuzz token-set) collapses cross-platform mirrors before the scorer; `CreditManager` caps + circuit-breaker; fail-soft fallback to legacy dedup. New deps: pydantic, httpx, rapidfuzz. Tests: dedup (8) + models (10) + relevance (8).
- 2026-06-23 — **Swiped jobs reappearing — root cause = infra.** Railway `job-hunter` has **no persistent volume**, so the SQLite DB + uploads are wiped on every redeploy. FIX (user action): attach a volume at `/data`, set `DATABASE_PATH=/data/jobs.db` + `UPLOADS_DIR=/data/uploads`. Code: search de-dup now matches **every** stored URL (any status/age); swipe writes surface a red error banner instead of swallowing failures.
- 2026-06-23 — **Mobile SPA fixes (consolidated).** Score explainer (Match vs Strength); finish-swipe→dashboard one-shot redirect (no bounce); View-CV streams the stored PDF; Match/Date/Company sort; CV filename+date; "Analyze my CV with AI"; "Check links" auto-rejects only definitively-dead URLs; refresh keeps you on the same screen (`sessionStorage` `jh.nav`).
- 2026-06-23 — 🚨 **Fixed unstyled app + locked in the build recipe.** SPA bundles were shipping a 393-byte CSS (Tailwind never compiled). **BUILD REQUIREMENT:** build in a sandbox copy that includes `tailwind.config.js` + `postcss.config.js` (+ `src/index.css`); confirm built CSS is ~25KB. The Railway deploy does NOT build the frontend — `web_bundle/` is committed and served as-is, so any `web/src` change needs a rebuild + commit of `web_bundle/`.
  - ⚠️ **Edit-tool writes to this mount can truncate files >~250 lines mid-write.** Prefer bash `cp`/Python writes + line-count / py_compile verification.
- 2026-06-22 — STATUS.md + CLAUDE.md seeded.
- (prior) — Gemini-only migration, apply mislabel fix, amber manual badge + honest summary, direct-ATS sourcing, apply-freeze fix.
                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          
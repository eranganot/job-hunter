# Job Hunter — Status

_Last updated: 2026-06-22 by Claude (session "review tasks / optimize")._
_Seeded from git history + prior transcripts._

## Now (live on Railway, branch main)
- All LLM calls on Gemini (Anthropic fully removed) — apply form-filling, confirmation, CV, cover letters, search.
- Apply mislabeling fixed: only marks "applied" when no error; auto-cleans errored "applied" jobs back to the queue.
- Apply-freeze fixed: 30s Playwright timeouts + deadline guard + guarded submit wrapper (no more stuck spinner).
- Manual-apply rows render **amber** with a person icon ("Manual Apply Needed"), not green "Applied"; Telegram/notification summary reads honestly (e.g. "0 auto-submitted — 15 need manual apply").
- Sourcing prefers **direct-ATS URLs** (`site:greenhouse.io OR lever.co OR comeet.com OR myworkdayjobs.com …`) over job-board aggregators.
- SPA queue actions: mark applied / remove / open, 6 reject reasons, apply-result detail + retry.
- HEAD `e24418d`.

## ⚠️ Folder hygiene (do this)
The OneDrive copy (`…\Eran's dev\Job Hunter`, branch `master`, commit `870c4eb`) has drifted. Salvage any unsynced edits, then archive it. See `migration-plans\PLAN_unify-job-hunter.md`.

## Next (confirm priority)
- Verify direct-ATS sourcing actually fills the Approved queue with auto-submittable jobs (was the open question last session).
- Optionally enable `APPLY_RESOLVE_CAREER_PAGE=1` and measure rescue rate on LinkedIn listings.

## Known sharp edges
Two-folder drift (being retired); sandbox can't push; Playwright browser binaries must be present in the Railway image for real submits.

## Changelog (newest first)
- 2026-06-28 (cleanup fix) — First boot cleanup kept 3 (Riskified 'product strategy director' all-words also matched 'Director, Product Strategy') and the queue refilled because the existing '[startup] moved N errored applied back to queue' step ran AFTER it and bounced applied jobs that still had apply_error. Fixes: keeper now ORDERED-substring (keeps 'Product Strategy Director', drops 'Director, Product Strategy'); cleanup UPDATE clears apply_error/failure fields; cleanup moved to run LAST in startup; added QUEUE_CLEANUP=force to re-run past the marker. Keeper logic re-verified. Suite 102 passed. Re-run: set QUEUE_CLEANUP=force, deploy, then remove the var.
- 2026-06-28 (boot cleanup) — Added an auth-free **one-time boot queue cleanup** in __main__ (the SYNC_API_KEY HTTP endpoint kept 401'ing on a key mismatch). Set env `QUEUE_CLEANUP=1` → on next boot it marks all queued jobs (new/approved/deferred) for the admin user as 'applied (manual)' EXCEPT a keep-list (default Limy.ai SPM + Riskified Product Strategy Director; override via QUEUE_CLEANUP_KEEP), writes an `app_flags.queue_cleanup_done` marker so it runs EXACTLY once (future searches safe even if the var stays), and logs '[cleanup] ONE-TIME DONE: kept N ...'. Remove the env var after. Keeper-matching verified against a simulated queue. Suite 102 passed.
- 2026-06-28 (queue cleanup) — Added `POST /api/sync/queue-cleanup` (SYNC_API_KEY auth, default DRY RUN): marks all jobs in status new/approved/deferred as 'applied (manual)' EXCEPT a keep-list (default: Limy.ai Senior Product Manager + Riskified Product Strategy Director), so externally-handled jobs leave the queue and are remembered. Also raised SQLite `busy_timeout` 10s->30s in db.get_db so reject/remove writes don't fail (and get swallowed by the SPA) under heavy search — the root cause of 'removed jobs come back'. Note: in-app reject/remove already sets status='rejected' + a rejected_pattern (server-side memory was fine); the gap was transient lock failures + the SPA's `.catch(()=>{})`. Full suite 102 passed.
- 2026-06-28 (apply-engine scoring) — Applied the same `thinkingConfig:{thinkingBudget:0}` fix to the shared Gemini helper in `apply_engine.py` (line ~486) so apply-time calls (extract_applicant_data, form-fill, confirmation) stop returning 'No JSON found'. 48 apply/AI tests pass; full suite green.
- 2026-06-28 (apply-decouple) — **Approving a job no longer auto-fires the apply engine.** `/api/jobs/<id>/approve` called `_trigger_apply_bg` unconditionally, and that helper never checked `auto_apply_enabled` — so approving during search/review launched Chromium + a real submit even with auto-apply OFF. Now the approve-trigger is gated by `auto_apply_enabled` (consistent with the scheduled apply path at line ~1861): auto-apply OFF → approve just shortlists; ON → approve auto-applies. 'Apply Now' (explicit) and scheduled apply unchanged. Confirmed: `run_job_search` never calls apply. Gemini scorer fix verified live (now '50 -> 17/21 passed' vs 0). Full suite 102 passed. Noted-not-fixed: occasional 'database is locked' (SQLite concurrency) and apply_engine `extract_applicant_data: No JSON found` (same thinking-budget pattern, in apply_engine.py — only matters when apply actually runs).
- 2026-06-28 (bugfix round) — **Audit of prod logs; 3 real bugs fixed.** (1) `_LV_COMPANIES` UnboundLocalError in TechMap discovery (defined AFTER its use) — Lever auto-discovery silently crashed every run; relocated the dict before the discovery block. (2) Gemini scorer returned `0 passed` on almost every batch ('AI response had no JSON array') — gemini-2.5-flash thinking was eating the output budget; added `thinkingConfig:{thinkingBudget:0}` (matching the pattern already used at line ~5065) to all 3 scoring calls + bumped maxOutputTokens. (3) Quieted httpx/httpcore INFO logging (the 'all logs are red' alarm was httpx + access logs on stderr, which Railway tags as error — they were 200s, not errors). Note: most red lines are benign stderr access logs. Known-not-fixed: big-tech endpoints Google v3 (404) / Apple (301→pagenotfound) / Microsoft (error) need live validation; Amazon works. Full suite 102 passed.
- 2026-06-28 (fix2) — **Big-Tech kept ON but Israel-scoped; fixed hybrid/remote gate.** User wants Big-Tech companies' *Israel-office* jobs and uses 'Hybrid' (not 'Remote'). Relevance gate reworked: geography is Israel-aware and work-mode words ('hybrid'/'remote') no longer count as a location (so 'Hybrid - London' is dropped); remote-anywhere passes ONLY if the user actually lists 'remote'/'anywhere' (derived from prefs, not hardcoded). Big-Tech adapters now query a real geographic location (skip work-mode words; default Israel) and Microsoft passes `lc`. `BIGTECH_ENABLED` back to default ON (gated). Smoke: MSFT-Herzliya & Google-TLV PM roles KEEP; US/abroad, hybrid-abroad, and TLV software-engineer DROP. Full suite 102 passed.
- 2026-06-23 (pm-5) — Swiped jobs reappearing. **Root cause = infra:** the Railway `job-hunter` service has **no persistent volume**, so the SQLite DB (`DATABASE_PATH` default = `BASE_DIR/jobs.db`) and `UPLOADS_DIR` are ephemeral — wiped on every redeploy/restart, taking swipes + uploaded CVs with them. FIX (user action): attach a volume at `/data`, set `DATABASE_PATH=/data/jobs.db` and `UPLOADS_DIR=/data/uploads`, redeploy. (Postgres/Redis services exist in the project but the app uses neither — pure SQLite.) Code fixes shipped alongside: (a) search de-dup now matches **every** stored job URL for the user (any status/age), so a passed/approved/applied job is never re-added as new — previously the query excluded `applied`/`rejected` and capped at 30 days; (b) swipe writes no longer swallow errors — `persistAction` shows a red "couldn’t save … pull to refresh" banner so failures are visible. Files: `app.py`, `web/src/App.tsx`; bundle `web_bundle/assets/index-BkhDYoNJ.js` (CSS still 25KB).
- 2026-06-28 (fix) — **Ingestion relevance gate: external sources now honor user preferences.** The new global Big-Tech endpoints were flooding results with worldwide / off-title roles that diluted preference-aligned jobs (search appeared to 'ignore preferences'). Added `ingestion/relevance.py`: a geography + title gate applied in `collect_external_sources` BEFORE jobs reach the scorer — keeps only jobs matching the user's locations (Israel-city aware + remote) and titles (substring / fuzzy>=80 / role-noun overlap). ON by default; `INGEST_RELEVANCE_GATE=0` disables it; `BIGTECH_ENABLED=0` is the instant kill-switch for the global sources. Tests: `tests/test_ingestion_relevance.py` (8). Full suite 101 passed.
- 2026-06-28 — **New `ingestion/` pipeline: multi-source sourcing + cross-platform fuzzy dedup.** Added a modular, state-machine ingestion package (`ingestion/`) wired into `app.py`'s search. Sources behind a uniform `SourceAdapter` interface: FREE Big-Tech internal JSON endpoints (Microsoft/Amazon/Google/Apple/Meta — everyone) + PAID aggregators/scrapers (TheirStack/Adzuna/Coresignal/Apify/JobSpy — **admin only**, gated by `users.role`). Unified Pydantic schema (`NormalizedJob`→`CanonicalJob`) normalizes every payload; multi-stage **fuzzy deduper** (canonical-URL → fingerprint → company-blocked rapidfuzz token-set match, host-aware threshold) collapses a role mirrored across career page + LinkedIn + aggregator into ONE before the scorer. Paid sources self-skip without keys (stub-but-wired via env vars); `CreditManager` enforces monthly caps + circuit-breaker fallback; `ProxyManager` does rotating residential proxies on admin runs. Integration is fail-soft: if the package errors, app.py falls back to the legacy exact-fingerprint dedup. New deps: pydantic, httpx, rapidfuzz (see requirements.txt). New env vars in `.env.example`. Tests: `tests/test_ingestion_dedup.py` (8) + `tests/test_ingestion_models.py` (10) — all green; full suite 93 passed. ⚠️ Build note: the Write/Edit tools **truncated files >~250 lines** on this mount this session — used Python file IO (read/replace/write) + py_compile verification instead.
- 2026-06-23 (pm-4) — Fixed **Swipe became unreachable / couldn’t run a new search.** The pm-2 finish→dashboard redirect was a passive effect that fired whenever `currentIndex >= reviewJobs.length`, so after swiping the stack, tapping **Swipe** bounced straight back to the dashboard (and the "Run a new search" button lives on the swipe screen). Fix: redirect is now a **one-shot** armed only by the actual last swipe (`finishedSwiping` ref, and not while a reject sheet is open); removed the "Opening dashboard…" spinner branch so an exhausted stack always shows the AllDoneScreen (Run search reachable). Opening Swipe with no new jobs now correctly shows All-done + Run search. Bundle `web_bundle/assets/index-Bhj0D1Si.js` (CSS still the correct 25KB `index-DQ6xwz81.css`). (`web/src/App.tsx`)
- 2026-06-23 (pm-3) — 🚨 **FIXED unstyled app.** Every SPA bundle I built this session shipped a 393-byte CSS (Tailwind never compiled) → the app rendered as raw unstyled HTML once deployed. Cause: the sandbox build dir was missing `tailwind.config.js` + `postcss.config.js`, so vite ran without Tailwind. Fix: copy BOTH config files (and `src/index.css`) into the build dir before `npm run build`. Correct CSS is ~25KB. Bundle now `web_bundle/assets/index-DQ6xwz81.css` + `index-CKTP69w-.js`.
  - **BUILD REQUIREMENT (do not forget):** the SPA cannot be built on the OneDrive/Windows mount (npm can’t create symlinks there). Build in a sandbox copy that includes: `package.json`, `package-lock.json`, `tsconfig*.json`, `vite.config.ts`, **`tailwind.config.js`**, **`postcss.config.js`**, `index.html`, `src/`, `public/`. Always confirm the built CSS is ~20KB+ (not a few hundred bytes) before syncing to `web_bundle/`.
- 2026-06-23 (pm-2) — More mobile SPA fixes. (1) Swipe card scores now have a **"What’s this?"** explainer clarifying Match Score (job→profile fit) vs Your Strength (fit adjusted for CV/experience; equal when no adjustments apply). (2) **Finishing a non-empty swipe stack now auto-routes to the dashboard** (queue tab) instead of the "All done" screen; empty stacks still show it so the Swipe button isn’t a dead bounce. (3) **View CV**: new authenticated `GET /api/cv` streams the stored PDF inline (`?download=1` to download); added a "View" link on the Settings CV card. (4) **Sort control (Match/Date/Company)** added above Queue/Applied/Deferred lists (client-side; needed new `foundDate` on UiJob). Files: `app.py`, `web/src/App.tsx`, `web/src/api/client.ts`; bundle rebuilt (`web_bundle/assets/index-BnlyXYA3.js`).
- 2026-06-23 (pm) — Mobile SPA CV + queue fixes. (1) CV card now shows the **original filename + upload date** (new `cv_filename`/`cv_uploaded_date` columns; surfaced via `/api/me`; upload endpoint persists + returns them). (2) Added **"Analyze my CV with AI"** button in Settings → runs `/api/cv-optimizer-analyze` (coach: score/strengths/improvements/ATS notes), loads last cached result on open. (3) Added **"Check links"** button in the queue → `POST /api/validate-links` checks New+Approved URLs concurrently and auto-rejects only *definitively* dead ones (404/410/DNS-gone); 403/429/timeouts kept as "unverified" to avoid nuking bot-blocked live jobs. Files: `db.py`, `auth.py`, `app.py`, `web/src/App.tsx`, `web/src/api/client.ts`; SPA bundle rebuilt (`web_bundle/assets/index-9DQLmPVR.js`).
  - ⚠️ **Edit-tool writes to this mount truncate files mid-write.** db.py/app.py/auth.py/App.tsx/client.ts/STATUS.md were all silently truncated this session. Workaround used: reconstruct from `git show HEAD:<file>`, re-apply changes via Python, write back with `cp`, then verify line counts + py_compile/esbuild. Prefer bash `cp`/Python writes over the Edit tool here until root-caused.
- 2026-06-23 — Landing/refresh behavior. Cold launch routes by new-jobs count: swipe if there are new jobs, dashboard (queue tab) if none. A page **refresh now keeps you on the same screen** (current view+tab persisted in `sessionStorage` under `jh.nav`); the new-jobs rule only fires on a fresh session. Push deep-links (`?view=`) still win. (`web/src/App.tsx`)
  - **Rebuilt the SPA bundle** (`web_bundle/`, now `assets/index-B-6hBYTU.js`). NOTE: the Railway deploy does NOT build the frontend — `web_bundle/` is committed and served as-is, so any `web/src` change requires a rebuild + commit of `web_bundle/`. (Old unreferenced `web_bundle/assets/index-*.js|css` can be pruned on Windows; the mount blocked deletion.)
- 2026-06-22 — STATUS.md + CLAUDE.md seeded.
- (prior) — Gemini-only migration, apply mislabel fix, amber manual badge + honest summary, direct-ATS sourcing, apply-freeze fix.

# Job Hunter - Python dependencies
# Core modules (http.server, sqlite3, json, urllib) are stdlib - no install needed.

# Browser automation for job application submission
playwright==1.49.1

# Email sending via Resend
resend==2.10.0

# Anti-bot stealth patches for Playwright
playwright-stealth==1.0.6

# Web Push (VAPID) notifications for the mobile PWA
pywebpush==2.3.0

# ── Ingestion pipeline (multi-source sourcing + fuzzy dedup) ──
# Unified Pydantic schema that normalizes Big-Tech endpoints vs aggregator APIs
pydantic>=2.5,<3
# Clean HTTP with proxy rotation + retries for the source adapters
httpx>=0.27
# C-accelerated fuzzy matching for cross-platform deduplication
rapidfuzz>=3.6
# OPTIONAL (admin/JobSpy adapter; pulls pandas — install only if you want it):
#   pip install python-jobspy

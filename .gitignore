# Secrets — never commit these
config.json
.env

# Database & uploads (stored on Railway persistent volume, not in git)
*.db
*.db-shm
*.db-wal
uploads/

# Sync / task output files
pending_jobs.json
applied_updates.json
approved_jobs.json
applied_updates.json
notify.json
users_config.json

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# macOS
.DS_Store
._*

# Editor
.vscode/
.idea/
*.swp
*.swo

# SPA build deps (source in web/, committed bundle in web_bundle/)
web/node_modules/
web/dist/

jobs.db
jobs.db-journal
__pycache__/

# Local SQLite + cache artifacts
jobs.db
jobs.db-journal
__pycache__/

# Local agent/session state -- noise on every git status, and noise is where a
# real modified file hides. Portable copies live in the account skill library.
.claude/

# Staging for files the device bridge cannot delete; remove by hand.
_to_delete/

# Desktop-app scratch: superseded plan drafts saved into the repo by the Claude app.
Claude outputs/
